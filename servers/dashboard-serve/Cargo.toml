[package]
name = "asolaria-server-dashboard-serve"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"
license = "MIT OR Apache-2.0"
authors = ["Asolaria Federation"]
description = "Asolaria acer dashboard Host-8 responder bin (:4949) — thread-per-conn, json=0 HBP default + ?format=json cold opt-in. Increment-1 read-only port of the super-asolaria-os-dashboard (health, canon-index). STAGED shadow, no writes, no fire, no cutover; live Node :4949 untouched."

[[bin]]
name = "dashboard-serve"
path = "src/main.rs"

# stdlib only — no dependencies (Host-8 zero-dep pattern; matches council-serve / recall-serve)
[dependencies]
