[package]
name = "asolaria-server-cosign-serve"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
license = "MIT OR Apache-2.0"
authors = ["Asolaria Federation"]
description = "Asolaria cosign shadow service · additive Python-daemon parity harness"

[[bin]]
name = "cosign-serve"
path = "src/main.rs"

[dependencies]
asolaria-server-cosign-ledger = { path = "../cosign-ledger" }
