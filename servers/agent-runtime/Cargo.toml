[package]
name = "asolaria-server-agent-runtime"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
license = "MIT OR Apache-2.0"
authors = ["Asolaria Federation"]
description = "Asolaria userspace server · agent supervisor / registry · Phase-2.5 demote target for kernel/core/src/agent_runtime/"

[lib]
crate-type = ["rlib"]

# Phase-2.5 placeholder dependencies — mirror what kernel/core/src/agent_runtime/mod.rs imports.
# Source uses `alloc::{string, vec}` + `core::sync::atomic` + `crate::syscall::AccessTier`.
# No external crates today.
[dependencies]
