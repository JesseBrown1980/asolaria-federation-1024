# Trilateral evidence and federation reality — 2026-07-11

Canonical doctrine:
[`HYPER-BECHS--the-third-set/TRILATERAL-REALITY-EVIDENCE-DOCTRINE-2026-07-11.md`](https://github.com/JesseBrown1980/HYPER-BECHS--the-third-set/blob/main/TRILATERAL-REALITY-EVIDENCE-DOCTRINE-2026-07-11.md)

## Preserve the actual federation

This repository's four-device federation, Acer/Liris roles, Falcon/Aether edge roles, cosign tiers,
and Host-8 migration plan remain exactly as implemented and documented. “Trilateral evidence” does
not rename the runtime topology.

```text
runtime federation -> acer + liris + falcon + aether, per code/receipts
shared evidence     -> origin seat + attack-verify seat + independent third execution
CI                  -> additional immutable-head verifier
```

## Old and new are both real in their scopes

- The old Node-era BigPickle/GULP/cube/GNN fabric has measured historical behavior and
  self-improvement receipts.
- This repository is the newer Rust Host-8 kernel/migration rung, with built crates and gated
  cutover state.

The new migration being incomplete must not erase the old measurements. Old measurements must not
be used to claim every Rust cell is already cut over.

## Evidence rules

- a build/test on Acer is `MEASURED_ACER`;
- an independent Liris implementation or rerun is `MEASURED_LIRIS`;
- an unrelated container/toolchain is `MEASURED_THIRD_SEAT`;
- Actions against the immutable commit is `MEASURED_CI`;
- a missing local route or device is `CANNOT_SEE`, not `FALSE`;
- a source file or server crate is `MEASURED_REPO`, not automatically live;
- `E=0`, `process_launch=0`, or held-safe state means gated—not imaginary.

## Reproducibility and authority

The repository already requires independent reproducible builds. Third-seat and CI evidence
strengthen that rule, but they do not grant Tier-3 authority, append cosigns, fire hardware, modify
the kernel ABI, or perform cutover.

## De-deflate / no-inflate

Reject both errors:

```text
"new migration is staged, so old system was not real"       -> false deflation
"kernel source exists, so every server is live everywhere"   -> false inflation
"one seat cannot reach another, so the other node is false"  -> false deflation
"address capacity equals resident agents"                    -> false inflation
```

The correct state is per component, per vantage, per commit, and per authority gate.
